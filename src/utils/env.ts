const adminPassword = process.env.NEXT_PUBLIC_ADMIN_PASSWORD;
const memberPassword = process.env.NEXT_PUBLIC_MEMBER_PASSWORD;
const apiUrl = process.env.NEXT_PUBLIC_API_URL;

export {
    adminPassword,
    memberPassword,
    apiUrl
}